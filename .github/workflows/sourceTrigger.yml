name: workflow_caller

on: 
    workflow_dispatch:
 
jobs:
  cypressrun:
    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.triggerstep.outputs.count }}
      output2: ${{ steps.triggerstep.outputs.count1 }}
    steps:
      - name: Invoke workflow and scrap output
        id: triggerstep
        uses: the-actions-org/workflow-dispatch@v4
        with:
          workflow: Another Workflow
          repo: annamalaiponnusamy/repo_02
          token: ${{ secrets.ACTIONS_KEY  }}
          workflow-logs: json-output
          wait-for-completion-timeout: 2m
          wait-for-completion-interval: 10s
          display-workflow-run-url-interval: 10s
          display-workflow-run-url-timeout: 2m 
      - name: Another step that can handle the result
        if: always()
        run: |
            echo "Another Workflow conclusion: ${{ steps.triggerstep.outputs.workflow-conclusion }}"
            echo "count=${{ steps.triggerstep.outputs.workflow-logs }}" >> $GITHUB_OUTPUT
            echo "count1=${{ steps.triggerstep.outputs.workflow-conclusion }}" >> $GITHUB_OUTPUT
            
  cypress-run2:
        needs:  cypressrun
        if: ${{ needs.cypressrun.outputs.output1 }} == '11success'
        runs-on: ubuntu-latest 
        steps: 
        - name: Another step that can handle the result
          run: |
            echo ${{needs.cypressrun.outputs.output1}}  
            echo ${{needs.cypressrun.outputs.output2}}
             
  
